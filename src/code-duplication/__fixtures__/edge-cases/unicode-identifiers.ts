const greeting = "Hello, world!";
const japanese = "日本語テキスト";
const emoji = "🚀 launching";
const mixed = "café résumé naïve";

function processText(input: string): string {
  return input.trim().toLowerCase();
}
