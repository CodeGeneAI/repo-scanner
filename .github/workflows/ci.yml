name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.13
      - run: bun install --frozen-lockfile
      - name: Lint
        run: bun run lint
      - name: Typecheck
        run: bun run typecheck
      - name: Unit tests
        run: bun run test:unit
      - name: Self-scan smoke test
        run: |
          bun src/bin.ts --all-detectors --path . --format json > /tmp/self-scan.json
          jq -e '.vcs.type == "git"' /tmp/self-scan.json > /dev/null
          jq -e '.inventory.languages | length > 0' /tmp/self-scan.json > /dev/null
          jq -e '.inventory.totalFiles > 0' /tmp/self-scan.json > /dev/null
